# Core AI agent frameworks
langchain>=0.3.0
langgraph>=0.2.0

# GitHub Models/Copilot - primary provider for this project (called via the
# OpenAI-compatible chat completions API, hence langchain-openai)
langchain-openai>=0.3.0

# Anthropic - fallback provider if a native Claude key is used instead
langchain-anthropic>=0.3.0
anthropic>=0.37.0

# Web app and data processing
streamlit>=1.38.0
pandas>=2.2.0
pyarrow>=17.0.0
duckdb>=1.0.0

# AI memory / vector storage
chromadb>=0.5.0

# Environment and validation
python-dotenv>=1.0.0
pydantic>=2.0.0

# Testing and visualization
pytest>=8.0.0
matplotlib>=3.8.0
plotly>=5.0.0
